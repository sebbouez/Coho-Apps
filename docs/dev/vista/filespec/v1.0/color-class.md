# Color Class

This class represents a color in the Vista file specification.

A color is defined as a hexadecimal value, like `#RRGGBB`.
It also support themed color reference, using the '@' prefix, like `@1`.
